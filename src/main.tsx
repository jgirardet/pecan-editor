import React from 'react'
import ReactDOM from 'react-dom'
import { PecanEditor } from "."

ReactDOM.render(
  <React.StrictMode>
    <PecanEditor />
  </React.StrictMode>,
  document.getElementById('root')
)
